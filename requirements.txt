flask==3.1.3
opencv-python==4.13.0.92
ultralytics==8.4.40
numpy==2.4.4
flask-cors==4.0.0
requests==2.31.0

# PyTorch - already installed
# torch==2.11.0
# torchvision==0.26.0

# For Python 3.14, we'll use a simple text detection approach
# without external OCR libraries for now
# You can add EasyOCR or PaddleOCR later when they support Python 3.14
